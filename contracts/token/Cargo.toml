[package]
name = "zing-token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20"

[dev-dependencies]
soroban-sdk = { version = "20", features = ["testutils"] }
